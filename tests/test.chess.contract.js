describe('ChessContract', function() {

  describe('Upload contract', function() {

    it.only('Should return abi', async function() {
       const result = await webclient.upload(ChessGame);
       chai.ex
       debugger
    });


  });
});
